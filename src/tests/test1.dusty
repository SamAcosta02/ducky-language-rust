program real_test;

vars
  length, width, area:int;

void calculate_area(length: int, width: int) {
  area = length * width;
  if (area > 30) {
    print!("Big Area!");
  };
};

begin
{
  length = 5;
  width = 10;
  calculate_area(length, width);
  print!(area);
}
end